#!/bin/sh

chmod +x test
chmod +x background

./background &
./background &
./background &
./background &
./background &

counter=0
while [ "$counter" -lt 100 ]
        do
          time ./test $counter
          counter=$((counter+1))
        done

sleep 100000000
